kind: Service
apiVersion: v1
metadata:
  name: etcd
  namespace: kube-system
  labels:
    app: etcd
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "2381"
spec:
  selector:
    app: etcd
  ports:
    - name: metrics
      port: 2381
      protocol: TCP
