kind: Service
apiVersion: v1
metadata:
  name: apiserver
  namespace: kube-system
  labels:
    app: apiserver
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/scheme: "https"
spec:
  selector:
    app: apiserver
  ports:
    - name: metrics
      port: 443
      protocol: TCP
