kind: Service
apiVersion: v1
metadata:
  name: scheduler
  namespace: kube-system
  labels:
    app: scheduler
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "10251"
spec:
  selector:
    app: scheduler
  ports:
    - name: metrics
      port: 10251
      protocol: TCP
