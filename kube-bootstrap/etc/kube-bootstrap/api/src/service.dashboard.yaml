kind: Service
apiVersion: v1
metadata:
  name: kubernetes-dashboard
  namespace: kube-system
  labels:
    app: dashboard
    kubernetes.io/cluster-service: "true"
spec:
  selector:
    app: dashboard
  ports:
    - port: 80
      targetPort: 9090
