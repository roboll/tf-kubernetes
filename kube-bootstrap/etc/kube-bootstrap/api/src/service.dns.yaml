kind: Service
apiVersion: v1
metadata:
  name: dns
  namespace: kube-system
  labels:
    app: dns
spec:
  clusterIP: 10.0.0.10
  selector:
    app: dns
  ports:
    - name: dns
      port: 53
      protocol: UDP
    - name: dns-tcp
      port: 53
      protocol: TCP
